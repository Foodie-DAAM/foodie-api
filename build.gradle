plugins {
    id 'io.wusa.semver-git-plugin' version '2.0.2'
}

group = 'net.sandrohc.foodie'

semver {
    snapshotSuffix = '<count>.g<sha><dirty>'
    dirtyMarker = '-dirty'
}

allprojects {
    group = 'net.sandrohc.foodie'
    version = semver.info
}